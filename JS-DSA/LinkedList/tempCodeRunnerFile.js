var firstUniqChar = function (s) {
  let num = -1;

  for (let index = 0; index < s.length; index++) {
    let currLatter = s[i];

    if (s.includes(currLatter)) {
      return (num = i);
    }
  }
};
